import React from 'react';

export default function ButtonElement(props) {
    return <span className='has-text-danger has-text-weight-bold'>Placeholder BUTTON</span>;
    // return <button className={props.element.class} onClick={props.element.onclick}>{props.content}</button>;
}
